(()=>{
 function money(v){try{return new Intl.NumberFormat('pl-PL',{style:'currency',currency:'PLN',maximumFractionDigits:0}).format(v||0)}catch{return Math.round(v||0)+' PLN'}}
 function stale(c){return Boolean(c?.derivedDataStale||c?.needsReanalysis||c?.valuationSuppressedBecauseStale)}
 function coinValue(c){if(stale(c))return 0;const vals=[c.value,c.estimatedValue,c.estimate,c.valuation,c.priceEstimate,c.marketValue,Number(c.estimateLow&&c.estimateHigh?(Number(c.estimateLow)+Number(c.estimateHigh))/2:0)].map(Number).filter(v=>Number.isFinite(v)&&v>0);return vals[0]||0}
 function esc(s){return String(s??'').replace(/[&<>]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[m]))}
 function init(){if(!location.pathname.endsWith('index.html')&&location.pathname!=='/'&&location.pathname!=='')return;if(!window.ApoMonet)return;const main=document.querySelector('main.shell'),hero=main?.querySelector('.hero'),grid=hero?.nextElementSibling;if(!main||!hero||!grid||document.getElementById('homePrivateSummary'))return;const state=ApoMonet.load(),coins=state.coins||[],albums=state.albums||[],valued=coins.filter(c=>coinValue(c)>0),sum=valued.reduce((s,c)=>s+coinValue(c),0),staleCount=coins.filter(stale).length,latest=[...coins].sort((a,b)=>new Date(b.updatedAt||b.createdAt||0)-new Date(a.updatedAt||a.createdAt||0)).slice(0,3);const sec=document.createElement('section');sec.id='homePrivateSummary';sec.className='card';sec.style.marginBottom='18px';sec.innerHTML=`<span class="eyebrow">PRYWATNE • TYLKO TO URZĄDZENIE</span><h2>Twoja kolekcja w skrócie</h2><div style="display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin:12px 0"><div class="detail"><b>${coins.length}</b><br><span class="muted">monet</span></div><div class="detail"><b>${albums.length}</b><br><span class="muted">albumów</span></div><div class="detail"><b>${valued.length?money(sum):'—'}</b><br><span class="muted">suma dostępnych wycen</span></div></div>${staleCount?`<p class="muted">${staleCount} ${staleCount===1?'rekord wymaga':'rekordy wymagają'} ponownej analizy i nie ${staleCount===1?'jest':'są'} wliczane do sumy.</p>`:''}${latest.length?'<p><b>Ostatnie:</b> '+latest.map(c=>esc(c.canonicalTitle||c.title||[c.nominal,c.ruler,c.year].filter(Boolean).join(' • ')||'Moneta')).join(' · ')+'</p>':'<p class="muted">Kolekcja jest jeszcze pusta.</p>'}<a class="btn secondary" href="collection.html">Otwórz kolekcję</a>`;grid.before(sec)}
 document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();
