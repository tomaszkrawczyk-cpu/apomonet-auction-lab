(() => {
  const LANGUAGE_KEY = "apomonet_language_v2";
  const CACHE_KEY = "apomonet_analysis_translation_cache_v4";
  const STRING_FIELDS = ["title","country","ruler","nominal","metal","mint","variant","grade","rarity","imageQualityNote","denominationEvidence","valuationNote","description","fullDescription"];
  const ARRAY_FIELDS = ["warnings","uncertaintyReasons","followUpQuestions"];
  const DETAIL_STRING_FIELDS = ["variant","kopickiRarity","obverseDetails","reverseDetails","legendPunctuation","fullDescription","visibleDateReading","obverseLegend","reverseLegend","mintmaster","gradeAssessment","edgeDescription"];
  const DETAIL_ARRAY_FIELDS = ["warnings","diagnosticFeatures","initialsAndMarks","priceCriticalFeatures","authenticitySignals","recommendedChecks"];
  const inflight = new Map();
  const clone=value=>{try{return JSON.parse(JSON.stringify(value))}catch{return value&&typeof value==='object'?{...value}:value}};
  function currentLanguage(){return window.ApoLanguageRegistry?.current?.()||window.ApoI18n?.current?.()||localStorage.getItem(LANGUAGE_KEY)||'pl'}
  function canTranslate(language){if(language==='pl')return false;const registry=window.ApoLanguageRegistry;if(registry?.isEnabled)return registry.isEnabled(language);return /^[a-z]{2,3}(?:-[A-Za-z0-9]+)?$/.test(String(language||''))}
  function flatten(record){const items=[],add=(key,value)=>{const text=String(value??'').trim();if(text)items.push({key,text})};for(const key of STRING_FIELDS)add(key,record?.[key]);for(const key of ARRAY_FIELDS)(Array.isArray(record?.[key])?record[key]:[]).forEach((text,index)=>add(`${key}.${index}`,text));for(const key of DETAIL_STRING_FIELDS)add(`detail.${key}`,record?.detail?.[key]);for(const key of DETAIL_ARRAY_FIELDS)(Array.isArray(record?.detail?.[key])?record.detail[key]:[]).forEach((text,index)=>add(`detail.${key}.${index}`,text));return items.slice(0,96)}
  function hash(value){let result=2166136261;for(let i=0;i<value.length;i++){result^=value.charCodeAt(i);result=Math.imul(result,16777619)}return(result>>>0).toString(36)}
  function loadCache(){try{const value=JSON.parse(localStorage.getItem(CACHE_KEY)||'{}');return value&&typeof value==='object'?value:{}}catch{return{}}}
  function saveCache(cache){try{const entries=Object.entries(cache).slice(-30);localStorage.setItem(CACHE_KEY,JSON.stringify(Object.fromEntries(entries)))}catch{}}
  const translatedArray=(source,prefix,translations)=>(Array.isArray(source)?source:[]).map((value,index)=>translations[`${prefix}.${index}`]||value);
  function apply(record,translations,language){const localized=clone(record)||{};for(const key of STRING_FIELDS)if(translations[key])localized[key]=translations[key];for(const key of ARRAY_FIELDS)if(Array.isArray(localized[key]))localized[key]=translatedArray(localized[key],key,translations);if(localized.detail&&typeof localized.detail==='object'){for(const key of DETAIL_STRING_FIELDS)if(translations[`detail.${key}`])localized.detail[key]=translations[`detail.${key}`];for(const key of DETAIL_ARRAY_FIELDS)if(Array.isArray(localized.detail[key]))localized.detail[key]=translatedArray(localized.detail[key],`detail.${key}`,translations)}localized.displayLanguage=language;localized.sourceLanguage=record?.sourceLanguage||'pl';return localized}
  function usefulTranslations(items,translations){const source=new Map(items.map(item=>[item.key,item.text]));let translated=0,changed=0;for(const[key,value]of Object.entries(translations||{})){const text=String(value||'').trim();if(!source.has(key)||!text)continue;translated++;if(text!==String(source.get(key)||'').trim())changed++}return{translated,changed}}
  async function requestTranslation(items,language){const response=await fetch('/api/translate-analysis',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({language,items})});let data;try{data=await response.json()}catch{throw new Error('Translation service returned an invalid response.')}if(!response.ok||!data?.ok)throw new Error(data?.error||'Analysis translation failed.');return data.translations||{}}
  async function localize(record,language=currentLanguage()){if(!record||language==='pl')return record;if(!canTranslate(language))throw new Error(`Unsupported display language: ${language}`);const items=flatten(record);if(!items.length)return record;const cacheKey=`${language}:${hash(JSON.stringify(items))}`,cache=loadCache();if(cache[cacheKey]){const quality=usefulTranslations(items,cache[cacheKey]);if(quality.translated&&quality.changed)return apply(record,cache[cacheKey],language);delete cache[cacheKey];saveCache(cache)}let pending=inflight.get(cacheKey);if(!pending){pending=requestTranslation(items,language);inflight.set(cacheKey,pending)}let translations;try{translations=await pending}finally{if(inflight.get(cacheKey)===pending)inflight.delete(cacheKey)}const quality=usefulTranslations(items,translations);if(!quality.translated||!quality.changed)throw new Error('Translation did not change the analysis content.');cache[cacheKey]=translations;saveCache(cache);return apply(record,translations,language)}
  function message(key,language=currentLanguage()){const base={translating:{pl:'Tłumaczę najważniejsze dane i opis monety…',en:'Translating the coin data and description…',de:'Münzdaten und Beschreibung werden übersetzt…',fr:'Traduction des données et de la description…'},failed:{pl:'Nie udało się przetłumaczyć opisu. Oryginalny wynik pozostaje dostępny.',en:'The description could not be translated. The original result remains available.',de:'Die Beschreibung konnte nicht übersetzt werden. Das Original bleibt verfügbar.',fr:'La description n’a pas pu être traduite. Le résultat original reste disponible.'},basicReady:{pl:'Etap 1 gotowy.',en:'Stage 1 complete.',de:'Stufe 1 abgeschlossen.',fr:'Étape 1 terminée.'},detailReady:{pl:'Analiza szczegółowa zakończona.',en:'Detailed analysis complete.',de:'Detailanalyse abgeschlossen.',fr:'Analyse détaillée terminée.'},restored:{pl:'Wróciłeś do analizy — korekta została naniesiona.',en:'You are back in the analysis — your correction has been applied.',de:'Sie sind zurück in der Analyse — die Korrektur wurde übernommen.',fr:'Vous êtes revenu à l’analyse — votre correction a été appliquée.'}};return base[key]?.[language]||base[key]?.en||base[key]?.pl||''}
  window.ApoAnalysisI18n={currentLanguage,canTranslate,flatten,apply,localize,message};
})();
