(()=>{function esc(s){return String(s??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;')}function mount(){if(!location.pathname.endsWith('albums.html')||!window.ApoMonet)return;const grid=document.querySelector('.album-grid');if(!grid)return;const draw=()=>{grid.querySelectorAll('[data-user-album="1"]').forEach(x=>x.remove());const albums=(ApoMonet.load().albums||[]).filter(a=>a&&a.id);for(const a of albums){const count=(ApoMonet.load().coins||[]).filter(c=>(c.albumIds||[]).includes(a.id)).length,el=document.createElement('a');el.className='album album-cover';el.dataset.userAlbum='1';el.href='user-album.html?id='+encodeURIComponent(a.id);el.innerHTML=`<div><span class="album-tag">Własny</span><h2>${esc(a.name)}</h2><p class="muted">${esc(a.description||'Własny album użytkownika.')}</p><div class="album-stats">${count} ${count===1?'moneta':'monet'} • dotknij, aby otworzyć</div></div><div class="cover-coins"><div class="cover-coin"></div><div class="cover-coin"></div><div class="cover-coin"></div><div class="cover-coin"></div></div>`;grid.appendChild(el)}};const btn=[...document.querySelectorAll('button')].find(b=>b.textContent.includes('Nowy album'));if(btn)btn.onclick=()=>{const name=prompt('Nazwa nowego albumu:');if(!name||!name.trim())return;const a=ApoMonet.createAlbum(name.trim());draw();if(a&&confirm('Album „'+a.name+'” został utworzony. Otworzyć go teraz?'))location.href='user-album.html?id='+encodeURIComponent(a.id)};draw();new MutationObserver(()=>{if(!grid.querySelector('[data-user-album="1"]')&&(ApoMonet.load().albums||[]).length)draw()}).observe(grid,{childList:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(mount,20));else setTimeout(mount,20)})();